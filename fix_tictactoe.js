const fs = require('fs');
let html = fs.readFileSync('public/games/tictactoe/index.html', 'utf8');

// Fix fullscreen
html = html.replace("const gameArea = document.getElementById('actual-game-container');", "const gameArea = document.getElementById('game-fullscreen-target');");

// Add Back Button and Stats Button in action bar/menu
const statsBtn = `<button onclick="openModal('statsModal')" class="action-btn pill-btn" data-tooltip="View Stats" style="background-color: #3b82f6; color: white; border-radius: 30px; border: none; cursor: pointer; margin-right: 15px;"><i class="bi bi-bar-chart-fill"></i> <span>Stats</span></button>`;
const backBtn = `<button onclick="location.href='/games/tictactoe/index.html'" class="action-btn circle-btn" data-tooltip="Back to Menu" style="background-color: #ef4444; color: white; border: none; cursor: pointer; margin-right:auto;"><i class="bi bi-arrow-left"></i></button>`;

html = html.replace('<div class="game-action-bar modern-action-bar" stylconst fs = require('fs');
let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("const gameArea = document.getElr" html = html.replfl
// Add Back Button and Stats Button in action bar/menu
const statsBtn = `<button onclick="openModal('statsModal')" class="action-btn pill-btn" data-tooltip="Vi!--const statsBtn = `<button onclick="openModal('statsMoptconst backBtn = `<button onclick="location.href='/games/tictactoe/index.html'" class="action-btn circle-btn" data-tooltip="Back to Menu" style="background-color: #ef4444; color: white; border: none; cursor: pointer; margin-right:auto;"><i class="bi bi-arrow-left"></i></button>`;

html = html.replace: 
html = html.replace('<div class="game-action-bar modern-action-bar" stylconst fs = require('fs');
let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("const gameArea = document.getElr" html = html.replfl
// Add Back Button and Stats Button in action bar/menx; let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html = html.reexhtml = html.repl b// Add Back Button and Stats Button in action bar/menu
const statsBtn =  cconst statsBtn = `<button onclick="openModal('statsMo/i
html = html.replace: 
html = html.replace('<div class="game-action-bar modern-action-bar" stylconst fs = require('fs');
let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("const gameArea = document.getElr" html = html.replfl
// Add Back Button and Stats Button in action bar/menx; let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html = html.reexhtml    html     </div>
      let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("const gameArea = docume; // Fix fullscreen
html = html.realhtml = htm center;// Add Back Button and Stats Button in action bar/menx; let hcelet html s<// Fix fullscreen
html = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html =ghhtml = html.repln>html = html.reexhtml = html.repl b// Add Back Button and Stats,0const statsBtn =  cconst statsBtn = `<button onclick="openModal('statsMo/i
html = html.eehtml = html.replace: 
html = html.replace('<div class="game-action-bar monthtml = html.replace(s<let hcelet html = fs.readFileSyle
// Fix fullscreen
html = html.replace("const gameArea = documed;// Fix fullscreen
html = html.re  html = html.replle// Add Back Button and Stats Button in action bar/menx; let hcelet html ay// Fix fullscreen
html = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html =lehtml = html.repl; html = html.reexhtml    html     </div>
      let hcelet html -d      let hcelet html = fs.readFileSyl.2// Fix fullscreen
html;">0</span>
      html = html.repl  html = html.realhtml = htm center;// Add Back Button and Statsdahtml = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html =ghhtml = html.repln>html = html.reexhtml = html.repl bx;htmlsor: pointer; width: 100%; margin-top: 20px;">Close</button>html = html.eehtml = html.replace: 
html = html.replace('<div class="game-action-bar monthtml = html.replace(s<let hcelet html = fs.readFileSyle
// Fix fullscreen
  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameArea = documed;// Fix fullscreen
html = html.re  html = htnthtml = html.reply html = html.re  html = html.replle// Add Back Button and Statsd.html = html.replace("co"color: #fff; margin-bo// Fix fullscreen
html =lehtml = html.repl; html = html.reexhtml    html     <bahtml =lehtml = html.repl; html = html.reexhtml    html     </d:       let hcelet html -d      let hcelet html = fs.readFileSyl.2pxhtml;">0</span>
      html = html.repl  html = html.realhtml = htm center;// Add;       html = henhtml =ghhtml = html.repln>html = html.reexhtml = html.repl bx;htmlsor: pointer; width: 100%; margin-top: 20px;">Close</button>html = html.eehtml = htm3 html = html.replace('<div class="game-action-bar monthtml = html.replace(s<let hcelet html = fs.readFileSyle
// Fix fullscreen
  html = html.replace('<div class  // Fix fullscreen
  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = html.re  h2)html =lehtml = html.repl; html = html.reexhtml    html     <bahtml =lehtml = html.repl; html = html.reexhtml    html     </d:       let hcelet html -d      let hcelet html#f      html = html.repl  html = html.realhtml = htm center;// Add;       html = henhtml =ghhtml = html.repln>html = html.reexhtml = html.repl bx;htmlsor: pointer; width: 100%; margin-top: 20px;">Close</butto  // Fix fullscreen
  html = html.replace('<div class  // Fix fullscreen
  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = html.re  h2)html =lehtml = html.repl; html = html.reegi  html = html.re??  html = html.replace('<div class="gal// Fix fullscolhtml = htmlnt-size:0.85rem; margin:0;">Climb the ELO ran  html = html.replace('<div class  // Fix fullscreen
  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = html.re  h2)html =lehtml = html.repl; html = html.reegi  html = html.re??  html = html.replace('<div class="gal// Fix fullscolhtml = htmlnt-size:0.85rem; margin:0;">Climb the ELO ran  html = html.replace('<div class  // Fix fullscreen
  html = html.replace('<div class="gal// Fix fullscreen
html = hton  html = html.replace('<div class="gal// Fix fullschohtml = html.replace("const gameAr    html = h <buttonhsi  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = html.re  h2)html =lehtml = html.repl; html = html.reegi  html = html.re??  html = html.replace('<div class="gal// Fix fullscolhtml = htmlnt-size:0.85rem; margin:0;">Climb the EL8)html = html.replace("const gameAr    html = h <buttonh c  html = html.replace('<div class="gal// Fix fullscreen
html = hton  html = html.replace('<div class="gal// Fix fullschohtml = html.replace("const gameAr    html = h <buttonhsi  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = h  html = hton  html = html.replace('<div class="gal// Fi9;html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = html.re  h2)hechtml = hton  html = html.replace('<div class="gal// Fix fullschohtml = html.replace("const gameAr    html = h <buttonhsi  html = html.replace('<div class="gal// Fix fullscreen
html = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = h  html = hton  html = html.replace('<div class="gal// Fi9;html = html.replace("const gameAr    html = h <buttonhbahtml = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = h  html = hto"chtml = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = h  html = hton  html = html.replace('<div class="gal// Fi9;html = html.replace("const gameAr    html = h <buttonhbahtml = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = html.re  html = htnthtml = html.reply html = h  html = hto"chtml = html.replace("const gameAr    html = h <buttonhtml = html.replace("const gameArea = documed;// Fix fd"html = htwidth: 100%;">Close</button>
    </div>
</div>
<script>
function saveLoadout() {
    localStorage.setItem('ttt_token', document.getElementById('tokenSelect').value);
    localStorage.setItem('ttt_theme', document.getElementById('boardThemeSelect').value);
    alert('Loadout Saved!');
    closeModal('cosmeticsModal');
}
// Load on init
document.addEventListener('DOMContentLoaded', () => {
   if(localStorage.getItem('ttt_token')) document.getElementById('tokenSelect').value = localStorage.getItem('ttt_token');
   if(localStorage.getItem('ttt_theme')) document.getElementById('boardThemeSelect').value = localStorage.getItem('ttt_theme');
});
</script>
`;
html = html.replace('</body>', cosmeticsModal + '\n</body>');

fs.writeFileSync('public/games/tictactoe/index.html', html);
